<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.lukawska</groupId>
        <artifactId>trainSmart</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>file-export</artifactId>

    <properties>
        <poi.version>5.5.0</poi.version>
        <pdfbox.version>3.0.6</pdfbox.version>
        <itext.version>5.5.13.4</itext.version>
        <openapi.baseName>fileexport</openapi.baseName>
        <openapi.enumPackage>${openapi.packagePrefix}.${openapi.baseName}.application.dto</openapi.enumPackage>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>itextpdf</artifactId>
            <version>${itext.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>
        <dependency>
            <groupId>org.lukawska</groupId>
            <artifactId>training-plan</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.lukawska</groupId>
            <artifactId>mailing</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-openapi-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${project.basedir}/src/main/resources/${openapi.baseName}api.yml</inputSpec>
                            <importMappings>
                                <importMapping>ExportFormat=${openapi.enumPackage}.ExportFormat</importMapping>
                            </importMappings>
                            <typeMappings>ExportFormat=${openapi.enumPackage}.ExportFormat</typeMappings>
                            <configOptions>
                                <generateConstructorWithAllArgs>true</generateConstructorWithAllArgs>
                            </configOptions>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>

spring:
  application:
    name: trainSmart
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/trainSmart
    username: lukawska
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
